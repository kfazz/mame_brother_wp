#!/bin/sh

# Abort on error.
set -e

#DEBHELPER#

DOCLNK=/usr/share/doc/xmame-svga
if test "$1" = upgrade \
    -a -L $DOCLNK \
    && dpkg --compare-versions "$2" lt-nl 0.146-4
then
    rm $DOCLNK
fi
